<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>
    <%= foods.name %>
  </title>
</head>

<body>
  <nav>
    <a href="/users/<%= user._id %>/users/" class="btn">View Users</a>
  </nav>
  <h1>list of Food</h1>
  <h1>
    <ul>
      <% foods.forEach( (food)=> { %>
        <li>
          <%= food.name %>
        </li>
        <% }) %>
    </ul>
  </h1>
</body>