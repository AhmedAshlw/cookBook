<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community page</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Community page</h1>

    <ul>
        <% users.forEach( (userr) => { %>
        <li>
            <a href="/users/<%= user._id %>/users/<%= userr._id %>">
                <%= userr.username %>
            </a>
        </li>
        <% }) %>
    </ul>

</body>
</html>